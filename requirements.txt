fastapi>=0.115.6
uvicorn>=0.30.6
websockets>=13.0
aiohttp>=3.10.11,<4.0
aiohttp-socks>=0.9.1
aiosqlite>=0.20.0
python-dotenv>=1.0.1
# uvloop gives 2-4x asyncio speedup on Linux/macOS (skipped on Windows)
uvloop>=0.20.0; sys_platform != "win32"
# orjson is 2-3x faster than stdlib json for the hot WS ingress path
orjson>=3.10.7
